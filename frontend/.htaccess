# Servir les assets Next.js en statique (cPanel / Apache + Passenger)
<IfModule mod_rewrite.c>
  RewriteEngine On

  RewriteCond %{REQUEST_URI} ^/_next/static/(.+)$ [NC]
  RewriteCond %{DOCUMENT_ROOT}/.next/static/%1 -f
  RewriteRule ^_next/static/(.+)$ .next/static/$1 [L]
</IfModule>
